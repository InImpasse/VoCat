name: carrier-bundle-sync-disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Automated third-party sync is disabled
        shell: bash
        run: |
          echo "::error title=Automated sync disabled::Review carrier bundle updates on a temporary upstream integration branch."
          exit 1
